mutation CreateEvent(
	$title: String!
	$description: String
	$maxReservations: Int
) {
	createEvent(
		title: $title
		description: $description
		maxReservations: $maxReservations
	) {
		error {
			field
			message
		}
		event {
			id
			title
			description
		}
	}
}
