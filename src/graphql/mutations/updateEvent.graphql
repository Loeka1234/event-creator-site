mutation UpdateEvent(
	$title: String!
	$description: String
	$id: Int!
	$maxReservations: Int
) {
	updateEvent(
		title: $title
		description: $description
		id: $id
		maxReservations: $maxReservations
	) {
		id
		title
		description
		creatorId
		amountReservations
		maxReservations
	}
}
