query EventById($id: Int!) {
	eventById(id: $id) {
		id
		title
		description
		amountReservations
		maxReservations
		creatorId
		creator {
			id
			username
			email
			createdAt
			updatedAt
		}
	}
}
